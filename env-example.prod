# ============================================
# Configurações de Produção - Youppie
# ============================================
# Copie este arquivo para .env.prod e preencha com seus valores reais
# cp env-example.prod .env.prod
NODE_ENV=production

# ============================================
# Banco de Dados PostgreSQL
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SUA_SENHA_SEGURA_AQUI
POSTGRES_DB=youppie

# ============================================
# API - NestJS
# ============================================
# URL pública da API (usada para gerar URLs de imagens e links)
# Exemplo: https://api.seudominio.com ou http://seu-ip:3003
API_URL=https://api.seudominio.com

# Porta da API (padrão: 3003)
API_PORT=3003

# Secret para JWT tokens (use um valor aleatório e seguro)
# Gere com: openssl rand -base64 32
JWT_SECRET=SEU_JWT_SECRET_SUPER_SEGURO_AQUI_GERE_UM_ALEATORIO

# Secret para JWT tokens do admin (separado do JWT de usuários)
# Gere com: openssl rand -base64 32
ADMIN_JWT_SECRET=SEU_ADMIN_JWT_SECRET_SUPER_SEGURO_AQUI_GERE_UM_ALEATORIO

# ============================================
# Frontend - Next.js
# ============================================
# URL pública do frontend (usada pelo Next.js para chamadas da API)
# Exemplo: https://seudominio.com ou http://seu-ip:3002
NEXT_PUBLIC_API_URL=https://api.seudominio.com

# Porta do frontend (padrão: 3002)
WEB_PORT=3002

# ============================================
# Stripe - Pagamentos
# ============================================
# Chave secreta do Stripe (backend)
# Obtenha em: https://dashboard.stripe.com/apikeys
# Use chaves de produção (sk_live_...) para produção
STRIPE_SECRET_KEY=sk_live_...

# Secret do webhook do Stripe (backend)
# Obtenha em: https://dashboard.stripe.com/webhooks
# Crie um endpoint apontando para: https://seudominio.com/webhooks/stripe
# Copie o "Signing secret" (whsec_...)
STRIPE_WEBHOOK_SECRET=whsec_...

# Chave pública do Stripe (frontend)
# Obtenha em: https://dashboard.stripe.com/apikeys
# Use chaves de produção (pk_live_...) para produção
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...

# Price IDs dos planos do Stripe
# Crie os produtos e preços no Stripe Dashboard e copie os IDs
# Obtenha em: https://dashboard.stripe.com/products
STRIPE_PRICE_ID_BASIC=price_...
STRIPE_PRICE_ID_PRO=price_...
STRIPE_PRICE_ID_PREMIUM=price_...
STRIPE_PRICE_ID_ENTERPRISE=price_...

# ============================================
# Opcional: Configurações Adicionais
# ============================================
# CORS - Origins permitidas (separadas por vírgula se múltiplas)
# IMPORTANTE: Configure isso em produção para segurança
# Exemplo: https://seudominio.com,https://www.seudominio.com
# Se não configurado em produção, o CORS será desabilitado por segurança
CORS_ORIGIN=https://seudominio.com

# Para logs mais detalhados em produção, descomente:
# LOG_LEVEL=info

# ============================================
# Segurança
# ============================================
# IMPORTANTE:
# - NUNCA commite o arquivo .env.prod no git
# - Use senhas fortes e únicas
# - Rotacione o JWT_SECRET periodicamente
# - Mantenha backups do banco de dados
# - Configure SSL/TLS em produção

